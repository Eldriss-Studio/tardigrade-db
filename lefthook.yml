pre-commit:
  parallel: true
  commands:
    fmt:
      run: cargo fmt --all -- --check
    clippy:
      run: just lint
    typos:
      run: typos
      skip:
        - merge
        - rebase

pre-push:
  commands:
    ci:
      run: just ci
